{
    "Logging": {
        "IncludeScopes": false,
        "Debug": {
            "LogLevel": {
                "Default": "Warning"
            }
        },
        "Console": {
            "LogLevel": {
                "Default": "Warning"
            }
        },
        "LogLevel": {
            "Default":  "Debug"
        }
    },
    "ConnectionStrings": {
        //The mssqllocaldb points to the local database instance. You can check this
        //by using the SQL Server Object Explorer from the View menu. If the name of
        //the database is not mssqllocaldb, change the name in the DataContext to match
        //the name of the (localdb)\<dbname> so that the connectionstring points to the
        //correct localdb.
        "ProductDataContext": "Server=(localdb)\\projectsv13;Database=Product;Trusted_Connection=True;MultipleActiveResultSets=true"
        //"ProductDataContext": "Server=(localdb)\\mssqllocaldb;Database=Product;Trusted_Connection=True;MultipleActiveResultSets=true"
    }
}
